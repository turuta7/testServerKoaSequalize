// const http = require('http');
// const config = require('./config');

// const { pid } = process;

// http.createServer().listen(config.server.port, () => {
//   console.log(
//     `Server started on http://localhost:${config.server.port}. Pid: ${pid}`,
//   );
// });
