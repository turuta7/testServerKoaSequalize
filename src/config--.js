// require('dotenv').config();

// const port = process.env.PORT || 3000;
// const env = process.env.NODE_ENV || 'production';

// if (!Number.isInteger(port)) {
//   throw new Error(`Port "${port}" must be a integer`);
// }

// const config = {
//   env,
//   server: {
//     port,
//   },
// };

// module.exports = config;
